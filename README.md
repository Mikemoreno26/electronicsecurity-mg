# Electronic Security M.G. – Sitio Web
Sitio corporativo desarrollado con **Next.js 14 + Tailwind CSS** y API de contacto integrada.

## 🚀 Scripts
- `npm run dev` → local en http://localhost:3000
- `npm run build` → build de producción
- `npm start` → servir el build

## 🔧 Variables de entorno
Crear `.env.local` (ver `.env.local.example`).

## 📤 Despliegue
Recomendado **Vercel** → conecta el repo, añade variables y apunta tu dominio.

© 2025 Electronic Security M.G.
